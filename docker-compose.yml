services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: ${PORT:-3000}
      PROXY_URL: ${PROXY_URL:-}
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
